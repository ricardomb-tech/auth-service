# Perfil de producción — sin defaults (AD-10): toda variable debe venir del
# entorno real. Si falta alguna, el arranque debe fallar en vez de usar un
# valor implícito.
spring.jpa.show-sql=false
logging.level.org.hibernate.SQL=warn
